{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "npx nuxi typecheck",
    "clean": "rm -r node_modules/ .nuxt/"
  },
  "dependencies": {
    "nuxt": "^3.12.2"
  },
  "devDependencies": {
    "typescript": "~5.4.5",
    "vue-tsc": "^1.8.27"
  }
}
